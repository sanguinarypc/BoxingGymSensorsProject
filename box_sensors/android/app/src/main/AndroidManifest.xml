<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- package="com.sanguinarypc.box_sensors"> -->

    <!-- Υποστήριξη όλων των screen densities -->
    <supports-screens
        android:anyDensity="true"
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true" />

    <!-- Δίκτυο -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- === Bluetooth (Android 12+) ===
         Δηλώνουμε ρητά ότι ΔΕΝ παραγουμε τοποθεσία από τα scan results.
         ΣΗΜ.: Με το neverForLocation ορισμένα BLE beacons μπορεί να φιλτραριστούν από τα scan results (όπως προειδοποιεί το doc). -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Προαιρετικά μόνο αν κάνεις device advertising -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" /> -->

    <!-- Χρησιμοποιείς BLE -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- === Legacy/συμβατότητα για παλαιότερα Android === -->
    <!-- Android 11 και κάτω: Bluetooth & Admin -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- Location μόνο για ≤ Android 11 (αν χρειαζόταν παλιότερα για σάρωση) -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <!-- Για Android 9 και κάτω: -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="28" />

    <!-- Foreground service (αν το χρησιμοποιείς για BLE σύνδεση/σάρωση με ειδοποίηση) -->
    <!-- <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />  -->
    <!-- Android 14+: δήλωσε και το αντίστοιχο permission για τον τύπο FGS που χρησιμοποιείς.
         Για συνδεδεμένη συσκευή (Bluetooth/USB/NFC) είναι αυτό: -->
    <!-- <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />  -->

    <!-- Αν εμφανίζεις notifications σε Android 13+ (π.χ. για Foreground Service), θα χρειαστεί runtime άδεια -->
    <!-- <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> -->

    <!-- Επιλογή για εξαιρέσεις Doze αν έχεις σχετικό flow -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name="${applicationName}"
        android:label="BoxSensors"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/NormalTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true">

            <!-- Εφαρμογή theme μόλις εκκινήσει το process -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ΜΗΝ διαγράψεις αυτό: χρειάζεται για Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Παράδειγμα Service ΜΟΝΟ αν έχεις foreground BLE service (τότε βάλε τον σωστό τύπο) -->
        <!--
        <service
            android:name=".MyBluetoothService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
        -->
    </application>
</manifest>
